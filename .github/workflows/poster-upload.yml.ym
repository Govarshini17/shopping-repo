name: Poster Upload Workflow

# Run the workflow on any push to the repository
on:
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show repo files (optional)
        run: ls -la

      - name: Poster Uploaded Successfully
        run: echo "Poster Uploaded Successfully"
